<UserControl x:Class="NiesPro.AdminPanel.Views.PaymentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid>
        <materialDesign:Card Padding="24" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel HorizontalAlignment="Center">
                <materialDesign:PackIcon Kind="CreditCard" Width="64" Height="64" 
                                       Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                <TextBlock Text="Monitoring des Paiements" 
                         Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                         HorizontalAlignment="Center" Margin="0,16,0,8"/>
                <TextBlock Text="Cette fonctionnalité sera implémentée prochainement" 
                         Style="{StaticResource MaterialDesignBody1TextBlock}"
                         HorizontalAlignment="Center" Opacity="0.7"/>
                <TextBlock Text="• Transactions en temps réel avec PCI-DSS" 
                         Style="{StaticResource MaterialDesignCaptionTextBlock}" Margin="0,16,0,2"/>
                <TextBlock Text="• Détection fraude et alertes sécurité" 
                         Style="{StaticResource MaterialDesignCaptionTextBlock}" Margin="0,2"/>
                <TextBlock Text="• Rapports financiers et analytics KPIs" 
                         Style="{StaticResource MaterialDesignCaptionTextBlock}" Margin="0,2"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>